<Window x:Class="SchoolTesting.Views.TestResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SchoolTesting.Helpers"
        Title="Результаты теста" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <local:BoolToColorConverter x:Key="Conv"/>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="{Binding ScoreText}" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="{Binding PercentageText}" FontSize="14"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding Answers}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="2" Background="{Binding IsCorrect, Converter={StaticResource Conv}}">
                        <StackPanel>
                            <TextBlock Text="{Binding QuestionText}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding YourAnswer, StringFormat='Ваш ответ: {0}'}" Foreground="#333"/>
                            <TextBlock Text="{Binding CorrectAnswer, StringFormat='Правильный ответ: {0}'}" Foreground="Green" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="3" Content="Закрыть" Width="100" Height="30" IsCancel="True" Margin="0,10,0,0"/>
    </Grid>
</Window>